/**
 * DRAFT SCAFFOLD — first real session adapter lands here (codex app-server:
 * thread/start → turn/start, notifications projected to SessionEvent,
 * turn/steer with expectedTurnId, turn/interrupt for abort).
 */
export type CodexSessionScaffold = "not_implemented";
