/**
 * DRAFT SCAFFOLD — second session adapter (bundled Pi SDK: createAgentSession,
 * prompt settlement + event projection; a missing API key must surface as a
 * typed unavailability, mirroring the optional-dependency not_found path).
 */
export type PiSessionScaffold = "not_implemented";
