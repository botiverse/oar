{
  "name": "oar",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --deny-warnings .",
    "check": "npm run typecheck && npm run lint"
  },
  "devDependencies": {
    "oxlint": "^1.77.0",
    "typescript": "^5.6.3"
  }
}
